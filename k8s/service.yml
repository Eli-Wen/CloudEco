apiVersion: v1
kind: Service
metadata:
  name: cloudeco-api-service
  namespace: cloudeco
  labels:
    app.kubernetes.io/name: cloudeco-api-service
    app.kubernetes.io/part-of: fit5225-a1
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: cloudeco-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
